<?xml version="1.0" encoding="utf-8"?>
<manifest package="org.zakariya.stickyheadersapp"
          xmlns:android="http://schemas.android.com/apk/res/android">

	<uses-permission android:name="android.permission.INTERNET"/>

	<application
		android:name=".StickyHeadersDemoApp"
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:supportsRtl="true"
		android:theme="@style/AppTheme"
		android:launchMode="singleTop">

		<activity android:name=".ui.MainActivity">
			<intent-filter>
				<action android:name="android.intent.action.MAIN"/>
				<category android:name="android.intent.category.LAUNCHER"/>
			</intent-filter>
		</activity>

		<activity
			android:name=".ui.AddressBookDemoActivity"
			android:parentActivityName=".ui.MainActivity"
			android:launchMode="singleTop"
			android:label="@string/title_address_book_demo">
			<meta-data
				android:name="android.support.PARENT_ACTIVITY"
				android:value=".ui.MainActivity"/>
		</activity>

		<activity
			android:name=".ui.HeaderCallbacksDemoActivity"
			android:parentActivityName=".ui.MainActivity"
			android:launchMode="singleTop"
			android:label="@string/title_simple_demo">
			<meta-data
				android:name="android.support.PARENT_ACTIVITY"
				android:value=".ui.MainActivity"/>
		</activity>

		<activity
			android:name=".ui.SectioningAdapterDemoActivity"
			android:parentActivityName=".ui.MainActivity"
			android:launchMode="singleTop"
			android:label="@string/title_sectioning_adapter_demo">
			<meta-data
				android:name="android.support.PARENT_ACTIVITY"
				android:value=".ui.MainActivity"/>
		</activity>

	</application>

</manifest>
